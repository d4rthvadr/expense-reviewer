/**
 * Notification resource type enumeration
 * Defines the types of resources that notifications can reference
 */
export enum NotificationResourceType {
  CATEGORY = 'CATEGORY',
  TRANSACTION = 'TRANSACTION',
  BUDGET = 'BUDGET',
  REVIEW = 'REVIEW',
  SYSTEM = 'SYSTEM',
}
