#!/bin/bash

echo "Running tests..."
cd backend && npm test

if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Commit aborted."
  exit 1
fi

echo "✅ Tests passed. Running lint-staged..."
npx lint-staged --debug
